# Yatube Social Network API

REST API для социальной сети Yatube, разработанное на Django REST Framework.

## 🚀 Возможности

- **📝 Посты** - Создание, редактирование, удаление постов
- **💬 Комментарии** - Вложенная система комментариев к постам
- **👥 Подписки** - Система подписок на других пользователей
- **🔐 Аутентификация** - JWT и Session аутентификация
- **🎯 Права доступа** - Гибкая система разрешений

## 📋 Требования

- Python 3.9+
- Django 4.2+
- Django REST Framework

## ⚙️ Установка

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/stanislav-backend/api_yatube.git
cd api_yatube
Создайте виртуальное окружение:

bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
Установите зависимости:

bash
pip install -r requirements.txt
Примените миграции:

bash
cd yatube_api
python manage.py migrate
Создайте суперпользователя:

bash
python manage.py createsuperuser
Запустите сервер:

bash
python manage.py runserver
📡 API Endpoints
🔐 Аутентификация
POST /api/v1/jwt/create/ - Получение JWT токена

POST /api/v1/jwt/refresh/ - Обновление JWT токена

📝 Посты
GET/POST /api/v1/posts/ - Список постов / Создание поста

GET/PUT/PATCH/DELETE /api/v1/posts/{id}/ - Детали поста

💬 Комментарии
GET/POST /api/v1/posts/{post_id}/comments/ - Комментарии к посту

GET/PUT/PATCH/DELETE /api/v1/posts/{post_id}/comments/{id}/ - Детали комментария

👥 Подписки
GET/POST /api/v1/follow/ - Список подписок / Подписка на пользователя
